[project]
name = "gilknocker"
keywords = ["GIL"]
requires-python = ">=3.7"

[project.urls]
homepage = "https://github.com/milesgranger/gil-knocker"
repository = "https://github.com/milesgranger/gil-knocker"

[build-system]
requires = ["maturin>=0.14,<0.15"]
build-backend = "maturin"
